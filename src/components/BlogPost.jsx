import { useState } from "react";

const BlogPost = ({ blog }) => {
  // Return = Enter HTML
  return (
    <article>
      <h1> Placeholder title </h1>
    </article>
  );
};

export default BlogPost;
